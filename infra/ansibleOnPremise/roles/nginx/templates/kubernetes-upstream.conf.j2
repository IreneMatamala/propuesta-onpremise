upstream kubernetes_apiservers {
    least_conn;
    server {{ hostvars['k8s-master-0'].ansible_host }}:6443 max_fails=3 fail_timeout=30s;
    server {{ hostvars['k8s-master-1'].ansible_host }}:6443 max_fails=3 fail_timeout=30s;
    keepalive 64;
}
