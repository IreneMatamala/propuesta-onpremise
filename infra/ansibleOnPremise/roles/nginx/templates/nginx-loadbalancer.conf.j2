stream {
    upstream kubernetes {
        server 10.0.0.10:6443;
        server 10.0.0.11:6443;
    }

    server {
        listen 6443;
        proxy_pass kubernetes;
        proxy_timeout 1s;
        proxy_connect_timeout 1s;
    }
}
